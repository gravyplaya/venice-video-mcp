node_modules/
dist/
*.log
.DS_Store
.env
.env.local
.env.*.local

.cursor/
.claude/settings.local.json

*.tsbuildinfo
coverage/
.nyc_output/

# Test deliverables / previews from production runs against this MCP.
# These end up at the repo root because shell sessions cd into it; not
# part of the published package.
*.mp4
preview/
